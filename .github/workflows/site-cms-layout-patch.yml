name: Patch Site CMS Layout

on:
  push:
    branches: [agent/site-cms-and-lookup]
    paths:
      - '.github/workflows/site-cms-layout-patch.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/site-cms-and-lookup
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: python3 tools/site_cms_layout_patch.py
      - name: Commit patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/DeLong.Web/Pages/Shared/_Layout.cshtml src/DeLong.Web/wwwroot/css/public.css
          git commit -m "feat: connect public layout to site settings"
          git push origin HEAD:agent/site-cms-and-lookup
